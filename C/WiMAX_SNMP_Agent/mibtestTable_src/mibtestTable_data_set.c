/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.19 $ of : mfd-data-set.m2c,v $
 *
 * $Id:$
 *
 */
/*
 * standard Net-SNMP includes
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header
 */
#include "mibtestTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table mibtestTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * MYTEST-EXAMPLE-MIB::mibtestTable is subid 1 of testModule.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.8072.2.10.1, length: 10
 */
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * mibtestTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * mibtestTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (mibtestTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
mibtestTable_undo_setup(mibtestTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:mibtestTable:mibtestTable_undo_setup",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> Setup mibtestTable undo.
     * set up mibtestTable undo information, in preparation for a set.
     * Undo storage is in (* mibtestColumn2_val_ptr )*
     */

    return rc;
}                               /* mibtestTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (mibtestTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
mibtestTable_undo(mibtestTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:mibtestTable:mibtestTable_undo", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:451:M: |-> mibtestTable undo.
     * mibtestTable undo information, in response to a failed set.
     * Undo storage is in (* mibtestColumn2_val_ptr )*
     */

    return rc;
}                               /* mibtestTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (mibtestTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
mibtestTable_undo_cleanup(mibtestTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:mibtestTable:mibtestTable_undo_cleanup",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:452:M: |-> Cleanup mibtestTable undo.
     * Undo storage is in (* mibtestColumn2_val_ptr )*
     */

    return rc;
}                               /* mibtestTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * mibtestTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param mibtestTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
mibtestTable_commit(mibtestTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:mibtestTable:mibtestTable_commit", "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit mibtestTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_MIBTESTCOLUMN1_FLAG) {
        save_flags &= ~COLUMN_MIBTESTCOLUMN1_FLAG;      /* clear mibtestColumn1 */
        /*
         * TODO:482:o: |-> commit column mibtestColumn1.
         */

        //MAx
//        rc = -1;
        rc = MFD_SUCCESS;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "mibtestTable column mibtestColumn1 commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo mibtestColumn1
             */
            rowreq_ctx->column_set_flags |= COLUMN_MIBTESTCOLUMN1_FLAG;
        }
    }

    if (save_flags & COLUMN_MIBTESTCOLUMN2_FLAG) {
        save_flags &= ~COLUMN_MIBTESTCOLUMN2_FLAG;      /* clear mibtestColumn2 */
        /*
         * TODO:482:o: |-> commit column mibtestColumn2.
         */
        //MAx
//        rc = -1;
        rc = MFD_SUCCESS;
        if (-1 == rc) {
            snmp_log(LOG_ERR,
                     "mibtestTable column mibtestColumn2 commit failed\n");
        } else {
            /*
             * set flag, in case we need to undo mibtestColumn2
             */
            rowreq_ctx->column_set_flags |= COLUMN_MIBTESTCOLUMN2_FLAG;
        }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
        snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n",
                 save_flags);
        return MFD_ERROR;
    }

    return rc;
}                               /* mibtestTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * mibtestTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param mibtestTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
mibtestTable_undo_commit(mibtestTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:mibtestTable:mibtestTable_undo_commit",
                "called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:485:M: |-> Undo mibtestTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */


    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
}                               /* mibtestTable_undo_commit */

/*
 * TODO:440:M: Implement mibtestTable node value checks.
 * TODO:450:M: Implement mibtestTable undo functions.
 * TODO:460:M: Implement mibtestTable set functions.
 * TODO:480:M: Implement mibtestTable commit functions.
 */
/*---------------------------------------------------------------------
 * MYTEST-EXAMPLE-MIB::mibtestEntry.mibtestColumn1
 * mibtestColumn1 is subid 2 of mibtestEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.8072.2.10.1.1.2
 * Description:
Test column 1
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param mibtestColumn1_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 *
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * mibtestTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
mibtestColumn1_check_value(mibtestTable_rowreq_ctx * rowreq_ctx,
                           long mibtestColumn1_val)
{
    DEBUGMSGTL(("verbose:mibtestTable:mibtestColumn1_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid mibtestColumn1 value.
     */

    return MFD_SUCCESS;         /* mibtestColumn1 value not illegal */
}                               /* mibtestColumn1_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (mibtestTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * mibtestTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
mibtestColumn1_undo_setup(mibtestTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:mibtestTable:mibtestColumn1_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup mibtestColumn1 undo.
     */
    /*
     * copy mibtestColumn1 data
     * set rowreq_ctx->undo->mibtestColumn1 from rowreq_ctx->data.mibtestColumn1
     */
    rowreq_ctx->undo->mibtestColumn1 = rowreq_ctx->data.mibtestColumn1;


    return MFD_SUCCESS;
}                               /* mibtestColumn1_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param mibtestColumn1_val
 *        A long containing the new value.
 */
int
mibtestColumn1_set(mibtestTable_rowreq_ctx * rowreq_ctx,
                   long mibtestColumn1_val)
{

    DEBUGMSGTL(("verbose:mibtestTable:mibtestColumn1_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set mibtestColumn1 value.
     * set mibtestColumn1 value in rowreq_ctx->data
     */
    //Max
    mibtestColumn1_cache[rowreq_ctx->tbl_idx.mibtestIndex] = mibtestColumn1_val;
    rowreq_ctx->data.mibtestColumn1 = mibtestColumn1_val;

    return MFD_SUCCESS;
}                               /* mibtestColumn1_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
mibtestColumn1_undo(mibtestTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:mibtestTable:mibtestColumn1_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up mibtestColumn1 undo.
     */
    /*
     * copy mibtestColumn1 data
     * set rowreq_ctx->data.mibtestColumn1 from rowreq_ctx->undo->mibtestColumn1
     */
    rowreq_ctx->data.mibtestColumn1 = rowreq_ctx->undo->mibtestColumn1;


    return MFD_SUCCESS;
}                               /* mibtestColumn1_undo */

/*---------------------------------------------------------------------
 * MYTEST-EXAMPLE-MIB::mibtestEntry.mibtestColumn2
 * mibtestColumn2 is subid 3 of mibtestEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.8072.2.10.1.1.3
 * Description:
column 2
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param mibtestColumn2_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 *
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * mibtestTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
mibtestColumn2_check_value(mibtestTable_rowreq_ctx * rowreq_ctx,
                           long mibtestColumn2_val)
{
    DEBUGMSGTL(("verbose:mibtestTable:mibtestColumn2_check_value",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid mibtestColumn2 value.
     */

    return MFD_SUCCESS;         /* mibtestColumn2 value not illegal */
}                               /* mibtestColumn2_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (mibtestTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * mibtestTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
mibtestColumn2_undo_setup(mibtestTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:mibtestTable:mibtestColumn2_undo_setup",
                "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup mibtestColumn2 undo.
     */
    /*
     * copy mibtestColumn2 data
     * set rowreq_ctx->undo->mibtestColumn2 from rowreq_ctx->data.mibtestColumn2
     */
    rowreq_ctx->undo->mibtestColumn2 = rowreq_ctx->data.mibtestColumn2;


    return MFD_SUCCESS;
}                               /* mibtestColumn2_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param mibtestColumn2_val
 *        A long containing the new value.
 */
int
mibtestColumn2_set(mibtestTable_rowreq_ctx * rowreq_ctx,
                   long mibtestColumn2_val)
{

    DEBUGMSGTL(("verbose:mibtestTable:mibtestColumn2_set", "called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set mibtestColumn2 value.
     * set mibtestColumn2 value in rowreq_ctx->data
     */
    mibtestColumn2_cache[rowreq_ctx->tbl_idx.mibtestIndex] = mibtestColumn2_val;
    rowreq_ctx->data.mibtestColumn2 = mibtestColumn2_val;

    return MFD_SUCCESS;
}                               /* mibtestColumn2_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
mibtestColumn2_undo(mibtestTable_rowreq_ctx * rowreq_ctx)
{

    DEBUGMSGTL(("verbose:mibtestTable:mibtestColumn2_undo", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up mibtestColumn2 undo.
     */
    /*
     * copy mibtestColumn2 data
     * set rowreq_ctx->data.mibtestColumn2 from rowreq_ctx->undo->mibtestColumn2
     */
    rowreq_ctx->data.mibtestColumn2 = rowreq_ctx->undo->mibtestColumn2;


    return MFD_SUCCESS;
}                               /* mibtestColumn2_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * mibtestTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-mibtestTable if you don't have dependencies)
 */
int
mibtestTable_check_dependencies(mibtestTable_rowreq_ctx * rowreq_ctx)
{
    int             rc = MFD_SUCCESS;

    DEBUGMSGTL(("internal:mibtestTable:mibtestTable_check_dependencies",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check mibtestTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
}                               /* mibtestTable_check_dependencies */

/** @} */
