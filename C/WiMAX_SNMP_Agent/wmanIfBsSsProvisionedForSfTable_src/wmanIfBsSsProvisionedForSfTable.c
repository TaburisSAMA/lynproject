/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 14170 $ of $ 
 *
 * $Id:$
 */
/** \page MFD helper for wmanIfBsSsProvisionedForSfTable
 *
 * \section intro Introduction
 * Introductory text.
 *
 */
/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "wmanIfBsSsProvisionedForSfTable.h"

#include <net-snmp/agent/mib_modules.h>

#include "wmanIfBsSsProvisionedForSfTable_interface.h"

oid             wmanIfBsSsProvisionedForSfTable_oid[] =
    { WMANIFBSSSPROVISIONEDFORSFTABLE_OID };
int             wmanIfBsSsProvisionedForSfTable_oid_size =
OID_LENGTH(wmanIfBsSsProvisionedForSfTable_oid);

wmanIfBsSsProvisionedForSfTable_registration
    wmanIfBsSsProvisionedForSfTable_user_context;

void            initialize_table_wmanIfBsSsProvisionedForSfTable(void);
void            shutdown_table_wmanIfBsSsProvisionedForSfTable(void);


/**
 * Initializes the wmanIfBsSsProvisionedForSfTable module
 */
void
init_wmanIfBsSsProvisionedForSfTable(void)
{
    DEBUGMSGTL(("verbose:wmanIfBsSsProvisionedForSfTable:init_wmanIfBsSsProvisionedForSfTable", "called\n"));

    /*
     * TODO:300:o: Perform wmanIfBsSsProvisionedForSfTable one-time module initialization.
     */

    /*
     * here we initialize all the tables we're planning on supporting
     */
    if (should_init("wmanIfBsSsProvisionedForSfTable"))
        initialize_table_wmanIfBsSsProvisionedForSfTable();

}                               /* init_wmanIfBsSsProvisionedForSfTable */

/**
 * Shut-down the wmanIfBsSsProvisionedForSfTable module (agent is exiting)
 */
void
shutdown_wmanIfBsSsProvisionedForSfTable(void)
{
    if (should_init("wmanIfBsSsProvisionedForSfTable"))
        shutdown_table_wmanIfBsSsProvisionedForSfTable();

}

/**
 * Initialize the table wmanIfBsSsProvisionedForSfTable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_wmanIfBsSsProvisionedForSfTable(void)
{
    wmanIfBsSsProvisionedForSfTable_registration *user_context;
    u_long          flags;

    DEBUGMSGTL(("verbose:wmanIfBsSsProvisionedForSfTable:initialize_table_wmanIfBsSsProvisionedForSfTable", "called\n"));

    /*
     * TODO:301:o: Perform wmanIfBsSsProvisionedForSfTable one-time table initialization.
     */

    /*
     * TODO:302:o: |->Initialize wmanIfBsSsProvisionedForSfTable user context
     * if you'd like to pass in a pointer to some data for this
     * table, allocate or set it up here.
     */
    /*
     * a netsnmp_data_list is a simple way to store void pointers. A simple
     * string token is used to add, find or remove pointers.
     */
    user_context =
        netsnmp_create_data_list("wmanIfBsSsProvisionedForSfTable", NULL,
                                 NULL);

    /*
     * No support for any flags yet, but in the future you would
     * set any flags here.
     */
    flags = 0;

    /*
     * call interface initialization code
     */
    _wmanIfBsSsProvisionedForSfTable_initialize_interface(user_context,
                                                          flags);
}                               /* initialize_table_wmanIfBsSsProvisionedForSfTable */

/**
 * Shutdown the table wmanIfBsSsProvisionedForSfTable 
 */
void
shutdown_table_wmanIfBsSsProvisionedForSfTable(void)
{
    /*
     * call interface shutdown code
     */
    _wmanIfBsSsProvisionedForSfTable_shutdown_interface
        (&wmanIfBsSsProvisionedForSfTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
wmanIfBsSsProvisionedForSfTable_rowreq_ctx_init
    (wmanIfBsSsProvisionedForSfTable_rowreq_ctx * rowreq_ctx,
     void *user_init_ctx)
{
    DEBUGMSGTL(("verbose:wmanIfBsSsProvisionedForSfTable:wmanIfBsSsProvisionedForSfTable_rowreq_ctx_init", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:210:o: |-> Perform extra wmanIfBsSsProvisionedForSfTable rowreq initialization. (eg DEFVALS)
     */

    return MFD_SUCCESS;
}                               /* wmanIfBsSsProvisionedForSfTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void
wmanIfBsSsProvisionedForSfTable_rowreq_ctx_cleanup
    (wmanIfBsSsProvisionedForSfTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:wmanIfBsSsProvisionedForSfTable:wmanIfBsSsProvisionedForSfTable_rowreq_ctx_cleanup", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:211:o: |-> Perform extra wmanIfBsSsProvisionedForSfTable rowreq cleanup.
     */
}                               /* wmanIfBsSsProvisionedForSfTable_rowreq_ctx_cleanup */

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
wmanIfBsSsProvisionedForSfTable_pre_request
    (wmanIfBsSsProvisionedForSfTable_registration * user_context)
{
    DEBUGMSGTL(("verbose:wmanIfBsSsProvisionedForSfTable:wmanIfBsSsProvisionedForSfTable_pre_request", "called\n"));

    /*
     * TODO:510:o: Perform wmanIfBsSsProvisionedForSfTable pre-request actions.
     */

    return MFD_SUCCESS;
}                               /* wmanIfBsSsProvisionedForSfTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param user_context
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
wmanIfBsSsProvisionedForSfTable_post_request
    (wmanIfBsSsProvisionedForSfTable_registration * user_context, int rc)
{
    DEBUGMSGTL(("verbose:wmanIfBsSsProvisionedForSfTable:wmanIfBsSsProvisionedForSfTable_post_request", "called\n"));

    /*
     * TODO:511:o: Perform wmanIfBsSsProvisionedForSfTable post-request actions.
     */

    /*
     * check to set if any rows were changed.
     */
    if (wmanIfBsSsProvisionedForSfTable_dirty_get()) {
        /*
         * check if request was successful. If so, this would be
         * a good place to save data to its persistent store.
         */
        if (MFD_SUCCESS == rc) {
            /*
             * save changed rows, if you haven't already
             */
        }

        wmanIfBsSsProvisionedForSfTable_dirty_set(0);   /* clear table dirty flag */
    }

    return MFD_SUCCESS;
}                               /* wmanIfBsSsProvisionedForSfTable_post_request */


/** @{ */
