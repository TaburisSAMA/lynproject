/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 15899 $ of $ 
 *
 * $Id:$
 */
/*
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 * ***                                                               ***
 * ***  NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE  ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THIS FILE DOES NOT CONTAIN ANY USER EDITABLE CODE.      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THE GENERATED CODE IS INTERNAL IMPLEMENTATION, AND      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***    IS SUBJECT TO CHANGE WITHOUT WARNING IN FUTURE RELEASES.   ***
 * ***                                                               ***
 * ***                                                               ***
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 */

/*
 * standard Net-SNMP includes 
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header 
 */
#include "wmanIfBsRegisteredSsTable.h"


#include <net-snmp/agent/table_container.h>
#include <net-snmp/library/container.h>

#include "wmanIfBsRegisteredSsTable_interface.h"

#include <ctype.h>

/**********************************************************************
 **********************************************************************
 ***
 *** Table wmanIfBsRegisteredSsTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * WMAN-IF-MIB::wmanIfBsRegisteredSsTable is subid 1 of wmanIfBsCps.
 * Its status is Current.
 * OID: .1.3.6.1.2.1.10.184.1.1.2.1, length: 12
 */
typedef struct wmanIfBsRegisteredSsTable_interface_ctx_s {

    netsnmp_container *container;
    netsnmp_cache  *cache;

    wmanIfBsRegisteredSsTable_registration *user_ctx;

    netsnmp_table_registration_info tbl_info;

    netsnmp_baby_steps_access_methods access_multiplexer;

} wmanIfBsRegisteredSsTable_interface_ctx;

static wmanIfBsRegisteredSsTable_interface_ctx
    wmanIfBsRegisteredSsTable_if_ctx;

static void    
_wmanIfBsRegisteredSsTable_container_init
(wmanIfBsRegisteredSsTable_interface_ctx * if_ctx);
static void    
_wmanIfBsRegisteredSsTable_container_shutdown
(wmanIfBsRegisteredSsTable_interface_ctx * if_ctx);


netsnmp_container *
wmanIfBsRegisteredSsTable_container_get(void)
{
    return wmanIfBsRegisteredSsTable_if_ctx.container;
}

wmanIfBsRegisteredSsTable_registration *
wmanIfBsRegisteredSsTable_registration_get(void)
{
    return wmanIfBsRegisteredSsTable_if_ctx.user_ctx;
}

wmanIfBsRegisteredSsTable_registration *
wmanIfBsRegisteredSsTable_registration_set
    (wmanIfBsRegisteredSsTable_registration * newreg)
{
    wmanIfBsRegisteredSsTable_registration *old =
        wmanIfBsRegisteredSsTable_if_ctx.user_ctx;
    wmanIfBsRegisteredSsTable_if_ctx.user_ctx = newreg;
    return old;
}

int
wmanIfBsRegisteredSsTable_container_size(void)
{
    return CONTAINER_SIZE(wmanIfBsRegisteredSsTable_if_ctx.container);
}

/*
 * mfd multiplexer modes
 */
static Netsnmp_Node_Handler _mfd_wmanIfBsRegisteredSsTable_pre_request;
static Netsnmp_Node_Handler _mfd_wmanIfBsRegisteredSsTable_post_request;
static Netsnmp_Node_Handler _mfd_wmanIfBsRegisteredSsTable_object_lookup;
static Netsnmp_Node_Handler _mfd_wmanIfBsRegisteredSsTable_get_values;
/**
 * @internal
 * Initialize the table wmanIfBsRegisteredSsTable 
 *    (Define its contents and how it's structured)
 */
void
_wmanIfBsRegisteredSsTable_initialize_interface
    (wmanIfBsRegisteredSsTable_registration * reg_ptr, u_long flags)
{
    netsnmp_baby_steps_access_methods *access_multiplexer =
        &wmanIfBsRegisteredSsTable_if_ctx.access_multiplexer;
    netsnmp_table_registration_info *tbl_info =
        &wmanIfBsRegisteredSsTable_if_ctx.tbl_info;
    netsnmp_handler_registration *reginfo;
    netsnmp_mib_handler *handler;
    int             mfd_modes = 0;

    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_wmanIfBsRegisteredSsTable_initialize_interface", "called\n"));


    /*************************************************
     *
     * save interface context for wmanIfBsRegisteredSsTable
     */
    /*
     * Setting up the table's definition
     */
    netsnmp_table_helper_add_indexes(tbl_info, ASN_INTEGER,
                                               /** index: ifIndex */
                                     ASN_OCTET_STR,
                                                 /** index: wmanIfBsSsMacAddress */
                                     0);

    /*
     * Define the minimum and maximum accessible columns.  This
     * optimizes retrival. 
     */
    tbl_info->min_column = WMANIFBSREGISTEREDSSTABLE_MIN_COL;
    tbl_info->max_column = WMANIFBSREGISTEREDSSTABLE_MAX_COL;

    /*
     * save users context
     */
    wmanIfBsRegisteredSsTable_if_ctx.user_ctx = reg_ptr;

    /*
     * call data access initialization code
     */
    wmanIfBsRegisteredSsTable_init_data(reg_ptr);

    /*
     * set up the container
     */
    _wmanIfBsRegisteredSsTable_container_init
        (&wmanIfBsRegisteredSsTable_if_ctx);
    if (NULL == wmanIfBsRegisteredSsTable_if_ctx.container) {
        snmp_log(LOG_ERR,
                 "could not initialize container for wmanIfBsRegisteredSsTable\n");
        return;
    }

    /*
     * access_multiplexer: REQUIRED wrapper for get request handling
     */
    access_multiplexer->object_lookup =
        _mfd_wmanIfBsRegisteredSsTable_object_lookup;
    access_multiplexer->get_values =
        _mfd_wmanIfBsRegisteredSsTable_get_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->pre_request =
        _mfd_wmanIfBsRegisteredSsTable_pre_request;
    access_multiplexer->post_request =
        _mfd_wmanIfBsRegisteredSsTable_post_request;


    /*************************************************
     *
     * Create a registration, save our reg data, register table.
     */
    DEBUGMSGTL(("wmanIfBsRegisteredSsTable:init_wmanIfBsRegisteredSsTable",
                "Registering wmanIfBsRegisteredSsTable as a mibs-for-dummies table.\n"));
    handler =
        netsnmp_baby_steps_access_multiplexer_get(access_multiplexer);
    reginfo =
        netsnmp_handler_registration_create("wmanIfBsRegisteredSsTable",
                                            handler,
                                            wmanIfBsRegisteredSsTable_oid,
                                            wmanIfBsRegisteredSsTable_oid_size,
                                            HANDLER_CAN_BABY_STEP |
                                            HANDLER_CAN_RONLY);
    if (NULL == reginfo) {
        snmp_log(LOG_ERR,
                 "error registering table wmanIfBsRegisteredSsTable\n");
        return;
    }
    reginfo->my_reg_void = &wmanIfBsRegisteredSsTable_if_ctx;

    /*************************************************
     *
     * set up baby steps handler, create it and inject it
     */
    if (access_multiplexer->object_lookup)
        mfd_modes |= BABY_STEP_OBJECT_LOOKUP;
    if (access_multiplexer->set_values)
        mfd_modes |= BABY_STEP_SET_VALUES;
    if (access_multiplexer->irreversible_commit)
        mfd_modes |= BABY_STEP_IRREVERSIBLE_COMMIT;
    if (access_multiplexer->object_syntax_checks)
        mfd_modes |= BABY_STEP_CHECK_OBJECT;

    if (access_multiplexer->pre_request)
        mfd_modes |= BABY_STEP_PRE_REQUEST;
    if (access_multiplexer->post_request)
        mfd_modes |= BABY_STEP_POST_REQUEST;

    if (access_multiplexer->undo_setup)
        mfd_modes |= BABY_STEP_UNDO_SETUP;
    if (access_multiplexer->undo_cleanup)
        mfd_modes |= BABY_STEP_UNDO_CLEANUP;
    if (access_multiplexer->undo_sets)
        mfd_modes |= BABY_STEP_UNDO_SETS;

    if (access_multiplexer->row_creation)
        mfd_modes |= BABY_STEP_ROW_CREATE;
    if (access_multiplexer->consistency_checks)
        mfd_modes |= BABY_STEP_CHECK_CONSISTENCY;
    if (access_multiplexer->commit)
        mfd_modes |= BABY_STEP_COMMIT;
    if (access_multiplexer->undo_commit)
        mfd_modes |= BABY_STEP_UNDO_COMMIT;

    handler = netsnmp_baby_steps_handler_get(mfd_modes);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject row_merge helper with prefix rootoid_len + 2 (entry.col)
     */
    handler = netsnmp_get_row_merge_handler(reginfo->rootoid_len + 2);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject container_table helper
     */
    handler =
        netsnmp_container_table_handler_get(tbl_info,
                                            wmanIfBsRegisteredSsTable_if_ctx.
                                            container,
                                            TABLE_CONTAINER_KEY_NETSNMP_INDEX);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject cache helper
     */
    if (NULL != wmanIfBsRegisteredSsTable_if_ctx.cache) {
        handler =
            netsnmp_cache_handler_get(wmanIfBsRegisteredSsTable_if_ctx.
                                      cache);
        netsnmp_inject_handler(reginfo, handler);
    }

    /*
     * register table
     */
    netsnmp_register_table(reginfo, tbl_info);

}                               /* _wmanIfBsRegisteredSsTable_initialize_interface */

/**
 * @internal
 * Shutdown the table wmanIfBsRegisteredSsTable
 */
void
_wmanIfBsRegisteredSsTable_shutdown_interface
    (wmanIfBsRegisteredSsTable_registration * reg_ptr)
{
    /*
     * shutdown the container
     */
    _wmanIfBsRegisteredSsTable_container_shutdown
        (&wmanIfBsRegisteredSsTable_if_ctx);
}

void
wmanIfBsRegisteredSsTable_valid_columns_set(netsnmp_column_info *vc)
{
    wmanIfBsRegisteredSsTable_if_ctx.tbl_info.valid_columns = vc;
}                               /* wmanIfBsRegisteredSsTable_valid_columns_set */

/**
 * @internal
 * convert the index component stored in the context to an oid
 */
int
wmanIfBsRegisteredSsTable_index_to_oid(netsnmp_index * oid_idx,
                                       wmanIfBsRegisteredSsTable_mib_index
                                       * mib_idx)
{
    int             err = SNMP_ERR_NOERROR;

    /*
     * temp storage for parsing indexes
     */
    /*
     * ifIndex(1)/InterfaceIndex/ASN_INTEGER/long(long)//l/A/w/e/R/d/H
     */
    netsnmp_variable_list var_ifIndex;
    /*
     * wmanIfBsSsMacAddress(1)/MacAddress/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/H
     */
    netsnmp_variable_list var_wmanIfBsSsMacAddress;

    /*
     * set up varbinds
     */
    memset(&var_ifIndex, 0x00, sizeof(var_ifIndex));
    var_ifIndex.type = ASN_INTEGER;
    memset(&var_wmanIfBsSsMacAddress, 0x00,
           sizeof(var_wmanIfBsSsMacAddress));
    var_wmanIfBsSsMacAddress.type = ASN_OCTET_STR;

    /*
     * chain temp index varbinds together
     */
    var_ifIndex.next_variable = &var_wmanIfBsSsMacAddress;
    var_wmanIfBsSsMacAddress.next_variable = NULL;


    DEBUGMSGTL(("verbose:wmanIfBsRegisteredSsTable:wmanIfBsRegisteredSsTable_index_to_oid", "called\n"));

    /*
     * ifIndex(1)/InterfaceIndex/ASN_INTEGER/long(long)//l/A/w/e/R/d/H 
     */
    snmp_set_var_value(&var_ifIndex, (u_char *) & mib_idx->ifIndex,
                       sizeof(mib_idx->ifIndex));

    /*
     * wmanIfBsSsMacAddress(1)/MacAddress/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/H 
     */
    snmp_set_var_value(&var_wmanIfBsSsMacAddress,
                       (u_char *) & mib_idx->wmanIfBsSsMacAddress,
                       mib_idx->wmanIfBsSsMacAddress_len *
                       sizeof(mib_idx->wmanIfBsSsMacAddress[0]));


    err = build_oid_noalloc(oid_idx->oids, oid_idx->len, &oid_idx->len,
                            NULL, 0, &var_ifIndex);
    if (err)
        snmp_log(LOG_ERR, "error %d converting index to oid\n", err);

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers(&var_ifIndex);

    return err;
}                               /* wmanIfBsRegisteredSsTable_index_to_oid */

/**
 * extract wmanIfBsRegisteredSsTable indexes from a netsnmp_index
 *
 * @retval SNMP_ERR_NOERROR  : no error
 * @retval SNMP_ERR_GENERR   : error
 */
int
wmanIfBsRegisteredSsTable_index_from_oid(netsnmp_index * oid_idx,
                                         wmanIfBsRegisteredSsTable_mib_index
                                         * mib_idx)
{
    int             err = SNMP_ERR_NOERROR;

    /*
     * temp storage for parsing indexes
     */
    /*
     * ifIndex(1)/InterfaceIndex/ASN_INTEGER/long(long)//l/A/w/e/R/d/H
     */
    netsnmp_variable_list var_ifIndex;
    /*
     * wmanIfBsSsMacAddress(1)/MacAddress/ASN_OCTET_STR/char(char)//L/a/w/e/R/d/H
     */
    netsnmp_variable_list var_wmanIfBsSsMacAddress;

    /*
     * set up varbinds
     */
    memset(&var_ifIndex, 0x00, sizeof(var_ifIndex));
    var_ifIndex.type = ASN_INTEGER;
    memset(&var_wmanIfBsSsMacAddress, 0x00,
           sizeof(var_wmanIfBsSsMacAddress));
    var_wmanIfBsSsMacAddress.type = ASN_OCTET_STR;

    /*
     * chain temp index varbinds together
     */
    var_ifIndex.next_variable = &var_wmanIfBsSsMacAddress;
    var_wmanIfBsSsMacAddress.next_variable = NULL;


    DEBUGMSGTL(("verbose:wmanIfBsRegisteredSsTable:wmanIfBsRegisteredSsTable_index_from_oid", "called\n"));

    /*
     * parse the oid into the individual index components
     */
    err = parse_oid_indexes(oid_idx->oids, oid_idx->len, &var_ifIndex);
    if (err == SNMP_ERR_NOERROR) {
        /*
         * copy out values
         */
        mib_idx->ifIndex = *((long *) var_ifIndex.val.string);
        /*
         * NOTE: val_len is in bytes, wmanIfBsSsMacAddress_len might not be
         */
        if (var_wmanIfBsSsMacAddress.val_len >
            sizeof(mib_idx->wmanIfBsSsMacAddress))
            err = SNMP_ERR_GENERR;
        else {
            memcpy(mib_idx->wmanIfBsSsMacAddress,
                   var_wmanIfBsSsMacAddress.val.string,
                   var_wmanIfBsSsMacAddress.val_len);
            mib_idx->wmanIfBsSsMacAddress_len =
                var_wmanIfBsSsMacAddress.val_len /
                sizeof(mib_idx->wmanIfBsSsMacAddress[0]);
        }


    }

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers(&var_ifIndex);

    return err;
}                               /* wmanIfBsRegisteredSsTable_index_from_oid */


/*
 *********************************************************************
 * @internal
 * allocate resources for a wmanIfBsRegisteredSsTable_rowreq_ctx
 */
wmanIfBsRegisteredSsTable_rowreq_ctx *
wmanIfBsRegisteredSsTable_allocate_rowreq_ctx(void *user_init_ctx)
{
    wmanIfBsRegisteredSsTable_rowreq_ctx *rowreq_ctx =
        SNMP_MALLOC_TYPEDEF(wmanIfBsRegisteredSsTable_rowreq_ctx);

    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:wmanIfBsRegisteredSsTable_allocate_rowreq_ctx", "called\n"));

    if (NULL == rowreq_ctx) {
        snmp_log(LOG_ERR, "Couldn't allocate memory for a "
                 "wmanIfBsRegisteredSsTable_rowreq_ctx.\n");
        return NULL;
    }

    rowreq_ctx->oid_idx.oids = rowreq_ctx->oid_tmp;

    rowreq_ctx->wmanIfBsRegisteredSsTable_data_list = NULL;

    /*
     * if we allocated data, call init routine
     */
    if (!(rowreq_ctx->rowreq_flags & MFD_ROW_DATA_FROM_USER)) {
        if (SNMPERR_SUCCESS !=
            wmanIfBsRegisteredSsTable_rowreq_ctx_init(rowreq_ctx,
                                                      user_init_ctx)) {
            wmanIfBsRegisteredSsTable_release_rowreq_ctx(rowreq_ctx);
            rowreq_ctx = NULL;
        }
    }

    return rowreq_ctx;
}                               /* wmanIfBsRegisteredSsTable_allocate_rowreq_ctx */

/*
 * @internal
 * release resources for a wmanIfBsRegisteredSsTable_rowreq_ctx
 */
void
wmanIfBsRegisteredSsTable_release_rowreq_ctx
    (wmanIfBsRegisteredSsTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:wmanIfBsRegisteredSsTable_release_rowreq_ctx", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    wmanIfBsRegisteredSsTable_rowreq_ctx_cleanup(rowreq_ctx);

    /*
     * free index oid pointer
     */
    if (rowreq_ctx->oid_idx.oids != rowreq_ctx->oid_tmp)
        free(rowreq_ctx->oid_idx.oids);

    SNMP_FREE(rowreq_ctx);
}                               /* wmanIfBsRegisteredSsTable_release_rowreq_ctx */

/**
 * @internal
 * wrapper
 */
static int
_mfd_wmanIfBsRegisteredSsTable_pre_request(netsnmp_mib_handler *handler,
                                           netsnmp_handler_registration
                                           *reginfo,
                                           netsnmp_agent_request_info
                                           *agtreq_info,
                                           netsnmp_request_info *requests)
{
    int             rc;

    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_mfd_wmanIfBsRegisteredSsTable_pre_request", "called\n"));

    if (1 != netsnmp_row_merge_status_first(reginfo, agtreq_info)) {
        DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable",
                    "skipping additional pre_request\n"));
        return SNMP_ERR_NOERROR;
    }

    rc = wmanIfBsRegisteredSsTable_pre_request
        (wmanIfBsRegisteredSsTable_if_ctx.user_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("wmanIfBsRegisteredSsTable", "error %d from "
                    "wmanIfBsRegisteredSsTable_pre_request\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }

    return SNMP_ERR_NOERROR;
}                               /* _mfd_wmanIfBsRegisteredSsTable_pre_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_wmanIfBsRegisteredSsTable_post_request(netsnmp_mib_handler *handler,
                                            netsnmp_handler_registration
                                            *reginfo,
                                            netsnmp_agent_request_info
                                            *agtreq_info,
                                            netsnmp_request_info *requests)
{
    wmanIfBsRegisteredSsTable_rowreq_ctx *rowreq_ctx =
        netsnmp_container_table_row_extract(requests);
    int             rc, packet_rc;

    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_mfd_wmanIfBsRegisteredSsTable_post_request", "called\n"));

    /*
     * release row context, if deleted
     */
    if (rowreq_ctx && (rowreq_ctx->rowreq_flags & MFD_ROW_DELETED))
        wmanIfBsRegisteredSsTable_release_rowreq_ctx(rowreq_ctx);

    /*
     * wait for last call before calling user
     */
    if (1 != netsnmp_row_merge_status_last(reginfo, agtreq_info)) {
        DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable",
                    "waiting for last post_request\n"));
        return SNMP_ERR_NOERROR;
    }

    packet_rc = netsnmp_check_all_requests_error(agtreq_info->asp, 0);
    rc = wmanIfBsRegisteredSsTable_post_request
        (wmanIfBsRegisteredSsTable_if_ctx.user_ctx, packet_rc);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("wmanIfBsRegisteredSsTable", "error %d from "
                    "wmanIfBsRegisteredSsTable_post_request\n", rc));
    }

    return SNMP_ERR_NOERROR;
}                               /* _mfd_wmanIfBsRegisteredSsTable_post_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_wmanIfBsRegisteredSsTable_object_lookup(netsnmp_mib_handler *handler,
                                             netsnmp_handler_registration
                                             *reginfo,
                                             netsnmp_agent_request_info
                                             *agtreq_info,
                                             netsnmp_request_info
                                             *requests)
{
    int             rc = SNMP_ERR_NOERROR;
    wmanIfBsRegisteredSsTable_rowreq_ctx *rowreq_ctx =
        netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_mfd_wmanIfBsRegisteredSsTable_object_lookup", "called\n"));

    /*
     * get our context from mfd
     * wmanIfBsRegisteredSsTable_interface_ctx *if_ctx =
     *             (wmanIfBsRegisteredSsTable_interface_ctx *)reginfo->my_reg_void;
     */

    if (NULL == rowreq_ctx) {
        rc = SNMP_ERR_NOCREATION;
    }

    if (MFD_SUCCESS != rc)
        netsnmp_request_set_error_all(requests, rc);
    else
        wmanIfBsRegisteredSsTable_row_prep(rowreq_ctx);

    return SNMP_VALIDATE_ERR(rc);
}                               /* _mfd_wmanIfBsRegisteredSsTable_object_lookup */

/***********************************************************************
 *
 * GET processing
 *
 ***********************************************************************/
/*
 * @internal
 * Retrieve the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_wmanIfBsRegisteredSsTable_get_column(wmanIfBsRegisteredSsTable_rowreq_ctx
                                      * rowreq_ctx,
                                      netsnmp_variable_list * var,
                                      int column)
{
    int             rc = SNMPERR_SUCCESS;

    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_mfd_wmanIfBsRegisteredSsTable_get_column", "called for %d\n", column));


    netsnmp_assert(NULL != rowreq_ctx);

    switch (column) {

        /*
         * wmanIfBsSsBasicCid(2)/WmanIfCidType/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSSBASICCID:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSsBasicCid_get(rowreq_ctx, (long *) var->val.string);
        break;

        /*
         * wmanIfBsSsPrimaryCid(3)/WmanIfCidType/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSSPRIMARYCID:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSsPrimaryCid_get(rowreq_ctx,
                                      (long *) var->val.string);
        break;

        /*
         * wmanIfBsSsSecondaryCid(4)/WmanIfCidType/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSSSECONDARYCID:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSsSecondaryCid_get(rowreq_ctx,
                                        (long *) var->val.string);
        break;

        /*
         * wmanIfBsSsManagementSupport(5)/INTEGER/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h 
         */
    case COLUMN_WMANIFBSSSMANAGEMENTSUPPORT:
        var->val_len = sizeof(u_long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSsManagementSupport_get(rowreq_ctx,
                                             (u_long *) var->val.string);
        break;

        /*
         * wmanIfBsSsIpManagementMode(6)/INTEGER/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h 
         */
    case COLUMN_WMANIFBSSSIPMANAGEMENTMODE:
        var->val_len = sizeof(u_long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSsIpManagementMode_get(rowreq_ctx,
                                            (u_long *) var->val.string);
        break;

        /*
         * wmanIfBsSs2ndMgmtArqEnable(7)/TruthValue/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h 
         */
    case COLUMN_WMANIFBSSS2NDMGMTARQENABLE:
        var->val_len = sizeof(u_long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSs2ndMgmtArqEnable_get(rowreq_ctx,
                                            (u_long *) var->val.string);
        break;

        /*
         * wmanIfBsSs2ndMgmtArqWindowSize(8)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSS2NDMGMTARQWINDOWSIZE:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSs2ndMgmtArqWindowSize_get(rowreq_ctx,
                                                (long *) var->val.string);
        break;

        /*
         * wmanIfBsSs2ndMgmtArqDnLinkTxDelay(9)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSS2NDMGMTARQDNLINKTXDELAY:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSs2ndMgmtArqDnLinkTxDelay_get(rowreq_ctx,
                                                   (long *) var->val.
                                                   string);
        break;

        /*
         * wmanIfBsSs2ndMgmtArqUpLinkTxDelay(10)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSS2NDMGMTARQUPLINKTXDELAY:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSs2ndMgmtArqUpLinkTxDelay_get(rowreq_ctx,
                                                   (long *) var->val.
                                                   string);
        break;

        /*
         * wmanIfBsSs2ndMgmtArqDnLinkRxDelay(11)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSS2NDMGMTARQDNLINKRXDELAY:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSs2ndMgmtArqDnLinkRxDelay_get(rowreq_ctx,
                                                   (long *) var->val.
                                                   string);
        break;

        /*
         * wmanIfBsSs2ndMgmtArqUpLinkRxDelay(12)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSS2NDMGMTARQUPLINKRXDELAY:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSs2ndMgmtArqUpLinkRxDelay_get(rowreq_ctx,
                                                   (long *) var->val.
                                                   string);
        break;

        /*
         * wmanIfBsSs2ndMgmtArqBlockLifetime(13)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/D/h 
         */
    case COLUMN_WMANIFBSSS2NDMGMTARQBLOCKLIFETIME:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSs2ndMgmtArqBlockLifetime_get(rowreq_ctx,
                                                   (long *) var->val.
                                                   string);
        break;

        /*
         * wmanIfBsSs2ndMgmtArqSyncLossTimeout(14)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/D/h 
         */
    case COLUMN_WMANIFBSSS2NDMGMTARQSYNCLOSSTIMEOUT:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSs2ndMgmtArqSyncLossTimeout_get(rowreq_ctx,
                                                     (long *) var->val.
                                                     string);
        break;

        /*
         * wmanIfBsSs2ndMgmtArqDeliverInOrder(15)/TruthValue/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h 
         */
    case COLUMN_WMANIFBSSS2NDMGMTARQDELIVERINORDER:
        var->val_len = sizeof(u_long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSs2ndMgmtArqDeliverInOrder_get(rowreq_ctx,
                                                    (u_long *) var->val.
                                                    string);
        break;

        /*
         * wmanIfBsSs2ndMgmtArqRxPurgeTimeout(16)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/D/h 
         */
    case COLUMN_WMANIFBSSS2NDMGMTARQRXPURGETIMEOUT:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSs2ndMgmtArqRxPurgeTimeout_get(rowreq_ctx,
                                                    (long *) var->val.
                                                    string);
        break;

        /*
         * wmanIfBsSs2ndMgmtArqBlockSize(17)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSS2NDMGMTARQBLOCKSIZE:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSs2ndMgmtArqBlockSize_get(rowreq_ctx,
                                               (long *) var->val.string);
        break;

        /*
         * wmanIfBsSsVendorIdEncoding(18)/OCTETSTR/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSSVENDORIDENCODING:
        var->type = ASN_OCTET_STR;
        rc = wmanIfBsSsVendorIdEncoding_get(rowreq_ctx,
                                            (char **) &var->val.string,
                                            &var->val_len);
        break;

        /*
         * wmanIfBsSsAasBroadcastPermission(19)/INTEGER/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h 
         */
    case COLUMN_WMANIFBSSSAASBROADCASTPERMISSION:
        var->val_len = sizeof(u_long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSsAasBroadcastPermission_get(rowreq_ctx,
                                                  (u_long *) var->val.
                                                  string);
        break;

        /*
         * wmanIfBsSsMaxTxPowerBpsk(20)/WmanIfMaxTxPowerType/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSSMAXTXPOWERBPSK:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSsMaxTxPowerBpsk_get(rowreq_ctx,
                                          (long *) var->val.string);
        break;

        /*
         * wmanIfBsSsMaxTxPowerQpsk(21)/WmanIfMaxTxPowerType/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSSMAXTXPOWERQPSK:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSsMaxTxPowerQpsk_get(rowreq_ctx,
                                          (long *) var->val.string);
        break;

        /*
         * wmanIfBsSsMaxTxPower16Qam(22)/WmanIfMaxTxPowerType/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSSMAXTXPOWER16QAM:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSsMaxTxPower16Qam_get(rowreq_ctx,
                                           (long *) var->val.string);
        break;

        /*
         * wmanIfBsSsMaxTxPower64Qam(23)/WmanIfMaxTxPowerType/ASN_INTEGER/long(long)//l/A/w/e/R/d/h 
         */
    case COLUMN_WMANIFBSSSMAXTXPOWER64QAM:
        var->val_len = sizeof(long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSsMaxTxPower64Qam_get(rowreq_ctx,
                                           (long *) var->val.string);
        break;

        /*
         * wmanIfBsSsMacVersion(24)/WmanIfMacVersion/ASN_INTEGER/long(u_long)//l/A/w/E/r/d/h 
         */
    case COLUMN_WMANIFBSSSMACVERSION:
        var->val_len = sizeof(u_long);
        var->type = ASN_INTEGER;
        rc = wmanIfBsSsMacVersion_get(rowreq_ctx,
                                      (u_long *) var->val.string);
        break;

    default:
        if (WMANIFBSREGISTEREDSSTABLE_MIN_COL <= column
            && column <= WMANIFBSREGISTEREDSSTABLE_MAX_COL) {
            DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_mfd_wmanIfBsRegisteredSsTable_get_column", "assume column %d is reserved\n", column));
            rc = MFD_SKIP;
        } else {
            snmp_log(LOG_ERR,
                     "unknown column %d in _wmanIfBsRegisteredSsTable_get_column\n",
                     column);
        }
        break;
    }

    return rc;
}                               /* _wmanIfBsRegisteredSsTable_get_column */

int
_mfd_wmanIfBsRegisteredSsTable_get_values(netsnmp_mib_handler *handler,
                                          netsnmp_handler_registration
                                          *reginfo,
                                          netsnmp_agent_request_info
                                          *agtreq_info,
                                          netsnmp_request_info *requests)
{
    wmanIfBsRegisteredSsTable_rowreq_ctx *rowreq_ctx =
        netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info *tri;
    u_char         *old_string;
    void            (*dataFreeHook) (void *);
    int             rc;

    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_mfd_wmanIfBsRegisteredSsTable_get_values", "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    for (; requests; requests = requests->next) {
        /*
         * save old pointer, so we can free it if replaced
         */
        old_string = requests->requestvb->val.string;
        dataFreeHook = requests->requestvb->dataFreeHook;
        if (NULL == requests->requestvb->val.string) {
            requests->requestvb->val.string = requests->requestvb->buf;
            requests->requestvb->val_len =
                sizeof(requests->requestvb->buf);
        } else if (requests->requestvb->buf ==
                   requests->requestvb->val.string) {
            if (requests->requestvb->val_len !=
                sizeof(requests->requestvb->buf))
                requests->requestvb->val_len =
                    sizeof(requests->requestvb->buf);
        }

        /*
         * get column data
         */
        tri = netsnmp_extract_table_info(requests);
        if (NULL == tri)
            continue;

        rc = _wmanIfBsRegisteredSsTable_get_column(rowreq_ctx,
                                                   requests->requestvb,
                                                   tri->colnum);
        if (rc) {
            if (MFD_SKIP == rc) {
                requests->requestvb->type = SNMP_NOSUCHINSTANCE;
                rc = SNMP_ERR_NOERROR;
            }
        } else if (NULL == requests->requestvb->val.string) {
            snmp_log(LOG_ERR, "NULL varbind data pointer!\n");
            rc = SNMP_ERR_GENERR;
        }
        if (rc)
            netsnmp_request_set_error(requests, SNMP_VALIDATE_ERR(rc));

        /*
         * if the buffer wasn't used previously for the old data (i.e. it
         * was allcoated memory)  and the get routine replaced the pointer,
         * we need to free the previous pointer.
         */
        if (old_string && (old_string != requests->requestvb->buf) &&
            (requests->requestvb->val.string != old_string)) {
            if (dataFreeHook)
                (*dataFreeHook) (old_string);
            else
                free(old_string);
        }
    }                           /* for results */

    return SNMP_ERR_NOERROR;
}                               /* _mfd_wmanIfBsRegisteredSsTable_get_values */


/***********************************************************************
 *
 * SET processing
 *
 ***********************************************************************/

/*
 * SET PROCESSING NOT APPLICABLE (per MIB or user setting)
 */
/***********************************************************************
 *
 * DATA ACCESS
 *
 ***********************************************************************/
static void     _container_free(netsnmp_container * container);

/**
 * @internal
 */
static int
_cache_load(netsnmp_cache * cache, void *vmagic)
{
    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_cache_load",
                "called\n"));

    if ((NULL == cache) || (NULL == cache->magic)) {
        snmp_log(LOG_ERR,
                 "invalid cache for wmanIfBsRegisteredSsTable_cache_load\n");
        return -1;
    }

    /** should only be called for an invalid or expired cache */
    netsnmp_assert((0 == cache->valid) || (1 == cache->expired));

    /*
     * call user code
     */
    return wmanIfBsRegisteredSsTable_container_load((netsnmp_container *)
                                                    cache->magic);
}                               /* _cache_load */

/**
 * @internal
 */
static void
_cache_free(netsnmp_cache * cache, void *magic)
{
    netsnmp_container *container;

    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_cache_free",
                "called\n"));

    if ((NULL == cache) || (NULL == cache->magic)) {
        snmp_log(LOG_ERR,
                 "invalid cache in wmanIfBsRegisteredSsTable_cache_free\n");
        return;
    }

    container = (netsnmp_container *) cache->magic;

    _container_free(container);
}                               /* _cache_free */

/**
 * @internal
 */
static void
_container_item_free(wmanIfBsRegisteredSsTable_rowreq_ctx * rowreq_ctx,
                     void *context)
{
    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_container_item_free",
                "called\n"));

    if (NULL == rowreq_ctx)
        return;

    wmanIfBsRegisteredSsTable_release_rowreq_ctx(rowreq_ctx);
}                               /* _container_item_free */

/**
 * @internal
 */
static void
_container_free(netsnmp_container * container)
{
    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_container_free",
                "called\n"));

    if (NULL == container) {
        snmp_log(LOG_ERR,
                 "invalid container in wmanIfBsRegisteredSsTable_container_free\n");
        return;
    }

    /*
     * call user code
     */
    wmanIfBsRegisteredSsTable_container_free(container);

    /*
     * free all items. inefficient, but easy.
     */
    CONTAINER_CLEAR(container,
                    (netsnmp_container_obj_func *) _container_item_free,
                    NULL);
}                               /* _container_free */

/**
 * @internal
 * initialize the container with functions or wrappers
 */
void
_wmanIfBsRegisteredSsTable_container_init
    (wmanIfBsRegisteredSsTable_interface_ctx * if_ctx)
{
    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_wmanIfBsRegisteredSsTable_container_init", "called\n"));

    /*
     * cache init
     */
    if_ctx->cache = netsnmp_cache_create(30,    /* timeout in seconds */
                                         _cache_load, _cache_free,
                                         wmanIfBsRegisteredSsTable_oid,
                                         wmanIfBsRegisteredSsTable_oid_size);

    if (NULL == if_ctx->cache) {
        snmp_log(LOG_ERR,
                 "error creating cache for wmanIfBsRegisteredSsTable\n");
        return;
    }

    if_ctx->cache->flags = NETSNMP_CACHE_DONT_INVALIDATE_ON_SET;

    wmanIfBsRegisteredSsTable_container_init(&if_ctx->container,
                                             if_ctx->cache);
    if (NULL == if_ctx->container)
        if_ctx->container =
            netsnmp_container_find
            ("wmanIfBsRegisteredSsTable:table_container");
    if (NULL == if_ctx->container) {
        snmp_log(LOG_ERR, "error creating container in "
                 "wmanIfBsRegisteredSsTable_container_init\n");
        return;
    }

    if (NULL != if_ctx->cache)
        if_ctx->cache->magic = (void *) if_ctx->container;
}                               /* _wmanIfBsRegisteredSsTable_container_init */

/**
 * @internal
 * shutdown the container with functions or wrappers
 */
void
_wmanIfBsRegisteredSsTable_container_shutdown
    (wmanIfBsRegisteredSsTable_interface_ctx * if_ctx)
{
    DEBUGMSGTL(("internal:wmanIfBsRegisteredSsTable:_wmanIfBsRegisteredSsTable_container_shutdown", "called\n"));

    wmanIfBsRegisteredSsTable_container_shutdown(if_ctx->container);

    _container_free(if_ctx->container);

}                               /* _wmanIfBsRegisteredSsTable_container_shutdown */


wmanIfBsRegisteredSsTable_rowreq_ctx *
wmanIfBsRegisteredSsTable_row_find_by_mib_index
    (wmanIfBsRegisteredSsTable_mib_index * mib_idx)
{
    wmanIfBsRegisteredSsTable_rowreq_ctx *rowreq_ctx;
    oid             oid_tmp[MAX_OID_LEN];
    netsnmp_index   oid_idx;
    int             rc;

    /*
     * set up storage for OID
     */
    oid_idx.oids = oid_tmp;
    oid_idx.len = sizeof(oid_tmp) / sizeof(oid);

    /*
     * convert
     */
    rc = wmanIfBsRegisteredSsTable_index_to_oid(&oid_idx, mib_idx);
    if (MFD_SUCCESS != rc)
        return NULL;

    rowreq_ctx =
        CONTAINER_FIND(wmanIfBsRegisteredSsTable_if_ctx.container,
                       &oid_idx);

    return rowreq_ctx;
}
