/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 1.49 $ of : mfd-top.m2c,v $
 *
 * $Id:$
 */
/** \mainpage MFD helper for mibtestTable
 *
 * \section intro Introduction
 * Introductory text.
 *
 */
/*
 * standard Net-SNMP includes
 */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/*
 * include our parent header
 */
#include "mibtestTable.h"

#include <net-snmp/agent/mib_modules.h>

#include "mibtestTable_interface.h"

oid             mibtestTable_oid[] = { MIBTESTTABLE_OID };
int             mibtestTable_oid_size = OID_LENGTH(mibtestTable_oid);

mibtestTable_registration mibtestTable_user_context;

void            initialize_table_mibtestTable(void);
void            shutdown_table_mibtestTable(void);


/**
 * Initializes the mibtestTable module
 */
void
init_mibtestTable(void)
{
    DEBUGMSGTL(("verbose:mibtestTable:init_mibtestTable", "called\n"));
    int i;

    /*
     * TODO:300:o: Perform mibtestTable one-time module initialization.
     */
    //Max 30/11--- it is not necessary for this array...
    for ( i = 0; i < mibtestTable_SIZE; i++ )
    {
    	mibtestColumn1_cache[ i ] = 0;
    	mibtestColumn2_cache[ i ] = 0;
    }
    //Max 30/11

    /*
     * here we initialize all the tables we're planning on supporting
     */
    if (should_init("mibtestTable"))
        initialize_table_mibtestTable();

}                               /* init_mibtestTable */

/**
 * Shut-down the mibtestTable module (agent is exiting)
 */
void
shutdown_mibtestTable(void)
{
    if (should_init("mibtestTable"))
        shutdown_table_mibtestTable();

}

/**
 * Initialize the table mibtestTable
 *    (Define its contents and how it's structured)
 */
void
initialize_table_mibtestTable(void)
{
    mibtestTable_registration *user_context;
    u_long          flags;

    DEBUGMSGTL(("verbose:mibtestTable:initialize_table_mibtestTable",
                "called\n"));

    /*
     * TODO:301:o: Perform mibtestTable one-time table initialization.
     */

    /*
     * TODO:302:o: |->Initialize mibtestTable user context
     * if you'd like to pass in a pointer to some data for this
     * table, allocate or set it up here.
     */
    /*
     * a netsnmp_data_list is a simple way to store void pointers. A simple
     * string token is used to add, find or remove pointers.
     */
    user_context = netsnmp_create_data_list("mibtestTable", NULL, NULL);

    /*
     * No support for any flags yet, but in the future you would
     * set any flags here.
     */
    flags = 0;

    /*
     * call interface initialization code
     */
    _mibtestTable_initialize_interface(user_context, flags);
}                               /* initialize_table_mibtestTable */

/**
 * Shutdown the table mibtestTable
 */
void
shutdown_table_mibtestTable(void)
{
    /*
     * call interface shutdown code
     */
    _mibtestTable_shutdown_interface(&mibtestTable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
mibtestTable_rowreq_ctx_init(mibtestTable_rowreq_ctx * rowreq_ctx,
                             void *user_init_ctx)
{
    DEBUGMSGTL(("verbose:mibtestTable:mibtestTable_rowreq_ctx_init",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:210:o: |-> Perform extra mibtestTable rowreq initialization. (eg DEFVALS)
     */

    return MFD_SUCCESS;
}                               /* mibtestTable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void
mibtestTable_rowreq_ctx_cleanup(mibtestTable_rowreq_ctx * rowreq_ctx)
{
    DEBUGMSGTL(("verbose:mibtestTable:mibtestTable_rowreq_ctx_cleanup",
                "called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:211:o: |-> Perform extra mibtestTable rowreq cleanup.
     */
}                               /* mibtestTable_rowreq_ctx_cleanup */

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
mibtestTable_pre_request(mibtestTable_registration * user_context)
{
    DEBUGMSGTL(("verbose:mibtestTable:mibtestTable_pre_request",
                "called\n"));

    /*
     * TODO:510:o: Perform mibtestTable pre-request actions.
     */

    return MFD_SUCCESS;
}                               /* mibtestTable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
mibtestTable_post_request(mibtestTable_registration * user_context, int rc)
{
    DEBUGMSGTL(("verbose:mibtestTable:mibtestTable_post_request",
                "called\n"));

    /*
     * TODO:511:o: Perform mibtestTable post-request actions.
     */

    /*
     * check to set if any rows were changed.
     */
    if (mibtestTable_dirty_get()) {
        /*
         * check if request was successful. If so, this would be
         * a good place to save data to its persistent store.
         */
        if (MFD_SUCCESS == rc) {
            /*
             * save changed rows, if you haven't already
             */
        }

        mibtestTable_dirty_set(0);      /* clear table dirty flag */
    }

    return MFD_SUCCESS;
}                               /* mibtestTable_post_request */


/** @{ */
